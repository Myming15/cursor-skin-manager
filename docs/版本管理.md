# 版本管理

- 当前版本：`0.1.11`
- `0.1.10` 新增整套光标角色替换、未分配文件重新分配与角色交换。
- `0.1.11` 修复操作成功提示持续显示，以及点击卡片任意区域误触发替换或分配的问题。
- 下一次功能新增、问题修复或正式发布从 `0.1.12` 开始递增。
- 后续每次发布都需要同步更新 `package.json`、`package-lock.json`、`src-tauri/Cargo.toml` 和 `src-tauri/tauri.conf.json`。
- 设置页通过 Tauri 运行时 API 读取版本，不在界面中硬编码正式版本号。
