# Cursor Skin Manager 社区开源建设 AI 执行步骤

## 1. 文档目的

本文档用于指导 AI 将 Cursor Skin Manager 从“公开源码并提供下载”逐步建设为“适合外部用户反馈、贡献者参与和维护者长期运营”的社区开源项目。

本文档只定义执行步骤，不代表这些步骤已经完成。后续执行时应一次只推进一个步骤，完成验证并向用户汇报后，再进入下一步。

当前基线：

- 项目版本：`0.1.11`
- 技术栈：Tauri 2、React、TypeScript、Rust
- 默认分支：`main`
- 开源许可：MIT License
- 已有内容：用户 README、Windows 安装版、便携版、SHA-256、GitHub Release、前端测试和 Rust 测试
- 当前维护者：Myming15

## 2. 本轮范围

本计划包括：

1. 社区贡献文档。
2. Issue、PR 和支持模板。
3. 安全策略与漏洞报告机制。
4. 开发者文档。
5. CI、质量门禁和依赖安全。
6. 分支与标签保护。
7. 版本、路线图和自动化发布。
8. Windows 代码签名方案。
9. 社区问题分流和长期维护机制。

本计划明确不包括：

- 第三方素材、角色、截图和光标包的版权处理。
- 在线市场、账号、收藏、标签和云同步等非 MVP 功能。
- 与社区建设无关的产品功能扩展。
- 未经用户确认的商业证书购买或付费服务开通。

## 3. AI 执行规则

1. 开始每一步前，先读取当前工作区和远程仓库状态，不假设文档仍与代码完全一致。
2. 不覆盖用户未提交的修改，不回滚与当前步骤无关的文件。
3. 每一步只修改该步骤直接涉及的文件和 GitHub 设置。
4. 每一步完成后都要执行对应验证，并报告新增文件、设置变化、测试结果和剩余风险。
5. 社区文档、模板和仓库设置本身不提升应用版本；只要生成了新的应用二进制或修改了应用行为，就必须从 `0.1.12` 开始递增版本。
6. GitHub Actions 权限使用最小权限，任何令牌、证书、密码和签名材料都不得写入仓库或日志。
7. 首次上线的自动化发布应生成 Draft Release，由维护者人工验证后再发布，不能直接自动推送正式安装包。
8. 在 CI 检查名称稳定之前，不设置依赖这些名称的强制分支规则。
9. 不修改已经公开发布的版本标签。需要修复发布内容时创建新版本，不移动旧标签。
10. 每个阶段优先通过独立分支和 Pull Request 完成，避免一次提交混合所有社区建设内容。

## 4. 前置决策

### 步骤 0：确认社区运营决策

**做什么**

在创建社区文档前，由用户确认项目接受什么形式的贡献，以及维护者如何与社区沟通。

需要确认：

| 决策 | 推荐选择 | 未确认时的处理 |
| --- | --- | --- |
| 是否接受外部 Pull Request | 接受 | 阻塞贡献指南定稿 |
| 社区语言 | 中文为主，关键模板提供中英双语字段 | 使用中文草案并标记待确认 |
| 安全漏洞联系方式 | GitHub Private Vulnerability Reporting 加专用邮箱 | 邮箱不得虚构，必须等待用户提供 |
| 普通使用问题渠道 | GitHub Issues | 默认使用 Issues |
| 是否立即开启 Discussions | 暂不开启 | 等问答量增加后再开启 |
| Release 发布方式 | CI 创建 Draft，维护者人工发布 | 使用 Draft 模式 |
| 维护者身份 | Myming15 | 使用当前仓库所有者 |
| 代码签名路线 | 先评估开源免费签名，再评估付费方案 | 不购买、不创建密钥 |

#### 决策结果（2026-07-13，已确认）

步骤状态：**已完成**

| 决策 | 已确认结果 |
| --- | --- |
| 是否接受外部 Pull Request | 接受外部 Pull Request |
| 社区语言 | 中文为主，Issue 和 PR 的关键字段提供中英双语 |
| 安全漏洞联系方式 | 暂时只使用 GitHub Private Vulnerability Reporting，不公开个人邮箱 |
| 普通使用问题渠道 | 使用 GitHub Issues |
| 是否立即开启 Discussions | 暂不开启，等问答量增加后再评估 |
| Release 发布方式 | CI 创建 Draft Release，由维护者验证后人工发布 |
| 维护者身份 | Myming15 |
| 代码签名路线 | 优先评估 SignPath Foundation 等开源免费方案，不自动购买服务 |

以上决定由用户在当前项目任务中明确确认。后续步骤不得擅自公开邮箱、开启 Discussions、自动发布正式 Release 或购买签名服务；需要改变这些决定时，应先取得用户新的明确确认并更新本记录。

**解决什么问题**

避免 AI 在贡献规则、联系方式和发布权限上做出未经授权的决定，也避免文档之间出现互相矛盾的承诺。

**主要产物**

- 一份已确认决策记录，可写入本计划的“决策结果”区域或单独的维护记录。

**验收标准**

- 外部 PR、社区语言和安全报告渠道都有明确答案。
- 没有在公开文件中写入未经确认的邮箱或响应承诺。

## 5. 阶段一：建立社区入口

### 步骤 1：完善 GitHub 仓库基本信息

**做什么**

完善 GitHub About 区域和仓库功能设置：

- 增加一句简洁的英文或中英双语项目描述。
- 设置项目 Topics，例如 `tauri`、`react`、`typescript`、`rust`、`windows`、`cursor`、`desktop-app`。
- 确认 Issues 已开启。
- 暂不启用 Discussions，除非用户决定启用。
- 检查默认分支、Release 链接和 README 下载链接。

#### 执行结果（2026-07-13）

步骤状态：**已完成**

| 检查项 | 执行结果 |
| --- | --- |
| About 描述 | 已设置中英双语项目描述，明确 Windows 本地光标皮肤管理定位和主要技术栈 |
| Homepage | 已指向 `https://github.com/Myming15/cursor-skin-manager/releases/latest` |
| Topics | 已设置 `tauri`、`react`、`typescript`、`rust`、`windows`、`cursor`、`desktop-app`、`cursor-theme`、`cursor-manager`、`windows-app` |
| 默认分支 | `main` |
| Issues | 已开启 |
| Discussions | 保持关闭 |
| 最新 Release | 公共链接正常，并正确跳转到 `v0.1.11` |
| README 入口 | “下载最新版本”和“提交问题”链接均可正常访问 |

以上远端设置已经通过 GitHub API 回读，并以未登录公共 URL 验证仓库主页、最新 Release 和 Issues 均返回 HTTP `200`。

**解决什么问题**

解决仓库难以被搜索发现、访客无法快速理解项目定位、反馈入口不明确的问题。

**主要产物**

- GitHub 仓库 About 描述。
- GitHub Topics。
- 已确认的 Issues 和 Discussions 状态。

**验收标准**

- 未登录用户打开仓库即可看到项目描述和技术标签。
- README 的“下载”和“提交问题”链接可正常访问。

### 步骤 2：建立贡献指南

**做什么**

创建 `CONTRIBUTING.md`，说明：

- 项目接受的贡献类型。
- 明确不接受的非 MVP 功能。
- 如何报告 Bug、提出功能建议和提交兼容性问题。
- Fork、分支、提交和 Pull Request 流程。
- 修改 React、Rust command、注册表逻辑、文件事务和 UI 时的要求。
- 提交前需要执行的测试与格式检查。
- PR 审查、修改请求和合并方式。
- 不得提交无授权光标包、密钥、用户数据和构建产物。

#### 执行结果（2026-07-13）

步骤状态：**已完成**

- 已在仓库根目录创建 `CONTRIBUTING.md`，覆盖贡献范围、问题报告、Windows 开发环境、分支与提交、实现约束、测试要求、PR 审查和禁止提交内容。
- 已在 README 顶部增加“参与贡献”入口；GitHub 可自动识别根目录贡献指南并在 Issue/PR 流程中展示。
- 已明确 Node.js 24 LTS 要求，并实际验证 `npm run test:ui`、`npm run build`、Rust 格式检查、15 项 Rust 测试和 `cargo check`。
- 已验证贡献指南引用的仓库路径和本地链接均存在，公开文件中没有写入个人邮箱。
- GitHub Private Vulnerability Reporting 已在步骤 4 启用，贡献指南现已直接指向安全策略和私密报告入口。

**解决什么问题**

解决贡献者不知道从哪里开始、提交内容偏离 MVP、代码风格与安全要求不一致的问题。

**主要产物**

- `CONTRIBUTING.md`

**验收标准**

- 新贡献者仅阅读该文件即可完成环境准备、创建分支、运行检查和提交 PR。
- GitHub 在创建 Issue 或 PR 时能够展示贡献指南入口。

### 步骤 3：建立行为准则和支持边界

**做什么**

创建：

- `CODE_OF_CONDUCT.md`
- `SUPPORT.md`

行为准则应说明社区交流规范、不可接受行为、处理方式和维护者责任。支持文档应区分：

- Bug 报告。
- 使用问题。
- 功能建议。
- 安全漏洞。
- 第三方光标包自身的问题。

#### 执行结果（2026-07-14）

步骤状态：**已完成**

- 已创建 `CODE_OF_CONDUCT.md`，明确适用范围、鼓励与禁止行为、公开和平台级举报路径、维护者责任、四级处置措施及申诉方式。
- 已创建 `SUPPORT.md`，区分应用 Bug、使用问题、功能建议、安全漏洞、行为准则事件和第三方光标包自身问题。
- 已在 README 增加“获取帮助”入口，并在贡献指南中加入行为准则与支持指南链接。
- 已验证 Issues、Security 和 GitHub Report Abuse 公共入口均可访问，所有本地文档链接均存在。
- 公开文档未写入个人邮箱，也未承诺固定回复、修复或处理时限；Private Vulnerability Reporting 已在步骤 4 启用。

**解决什么问题**

解决社区交流缺少边界、维护者需要反复解释反馈渠道、普通问题与安全问题混在一起的问题。

**主要产物**

- `CODE_OF_CONDUCT.md`
- `SUPPORT.md`

**验收标准**

- 行为准则包含明确的执行和联系机制。
- 支持文档能够引导用户选择正确渠道。
- 不承诺维护者无法长期保证的响应时间。

### 步骤 4：建立安全策略

**做什么**

创建 `SECURITY.md` 并配置 GitHub 安全报告功能：

- 说明当前接受安全修复的版本。
- 说明普通 Bug 与安全漏洞的区别。
- 禁止在公开 Issue 中披露可利用细节、用户隐私和敏感日志。
- 配置 GitHub Private Vulnerability Reporting。
- 用户提供专用安全邮箱后，再把邮箱写入安全策略。
- 定义确认收到、初步评估、修复和披露的大致流程。

#### 执行结果（2026-07-14）

步骤状态：**已完成**

- 已创建根目录 `SECURITY.md`，明确仅支持最新公开版本；当前受支持版本为 `v0.1.11`，`v0.1.10` 及更早版本不再支持。
- 已区分普通 Bug 与安全漏洞，说明私密报告所需信息、禁止公开内容、善意研究边界，以及确认、评估、修复、发布和协调披露流程。
- 已通过 GitHub API 启用 Private Vulnerability Reporting，并回读确认状态为启用；未登录 Security 页面已显示 “Report a vulnerability” 入口。
- 已将 README、贡献指南、支持文档和行为准则统一链接到 `SECURITY.md` 或私密报告入口。
- 未公开个人安全邮箱，也未承诺固定确认、修复或披露时限。
- `SECURITY.md` 已通过阶段一 PR #1 合并到 `main`；默认分支 Security 页面现已显示完整策略和私密报告入口。

**解决什么问题**

解决漏洞只能公开提交、攻击细节提前曝光、维护者无法协调修复和披露的问题。

**主要产物**

- `SECURITY.md`
- 已启用的 Private Vulnerability Reporting。
- 支持版本说明。

**验收标准**

- GitHub Security 页面能够显示安全策略。
- 未登录普通用户可以看到如何安全报告漏洞。
- 私密漏洞报告入口可用。

### 步骤 5：建立 Issue 和 Pull Request 模板

**做什么**

创建 `.github` 社区模板：

- `.github/ISSUE_TEMPLATE/bug_report.yml`
- `.github/ISSUE_TEMPLATE/feature_request.yml`
- `.github/ISSUE_TEMPLATE/cursor_compatibility.yml`
- `.github/ISSUE_TEMPLATE/config.yml`
- `.github/PULL_REQUEST_TEMPLATE.md`

Bug 模板要求应用版本、Windows 版本、安装方式、复现步骤、实际结果、期望结果和日志。兼容性模板额外要求皮肤包结构、文件格式、是否包含 INF，以及可安全分享的最小样本。

#### 执行结果（2026-07-14）

步骤状态：**已完成**

- 已创建 `bug_report.yml`、`feature_request.yml` 和 `cursor_compatibility.yml` 三类中英双语 Issue Form。
- Bug 表单必填应用版本、Windows 环境、安装方式、影响功能、复现步骤、期望结果、实际结果和日志或补充信息。
- 光标兼容性表单额外必填皮肤包结构、文件格式、INF 状态、复现结果和 Windows 对比，并提供有权分享的最小样本字段。
- 已创建 `config.yml`：保留空白 Issue 供使用问题使用，提供私密安全报告和 README 常见问题 Contact Link，不创建公开安全漏洞表单。
- 已创建 `PULL_REQUEST_TEMPLATE.md`，要求说明影响范围、自动与手动测试、UI 证据、数据安全影响、兼容性、风险和回滚。
- 四个 YAML 文件已通过 `js-yaml` 解析和结构检查，三个 Issue Form 的元素数量、唯一 ID、现有标签及外部链接均验证通过。
- 模板已通过阶段一 PR #1 以 squash 方式合并到 `main`，合并提交为 `14d1535`；默认分支已包含三个 Issue Form、模板配置和 PR 模板。
- GitHub Contents API 已回读四个 Issue 模板目录文件，Pull Request 模板也被 Community Profile 识别。Community Profile 不统计 YAML Issue Forms，Tauri 和 Vite 等使用 Issue Forms 的仓库同样返回 `issue_template=false`，因此不将该字段作为失败依据。

**解决什么问题**

解决 Issue 信息不完整、维护者来回追问、不同问题无法分类，以及 PR 缺少测试说明的问题。

**主要产物**

- 三类 Issue Form。
- Issue 模板配置。
- Pull Request 模板。

**验收标准**

- GitHub 新建 Issue 页面能显示三种明确入口。
- Bug 表单包含必填复现信息。
- PR 模板要求说明影响范围、测试结果、截图和数据安全影响。
- 安全漏洞入口指向私密报告，而不是公开 Issue。

## 6. 阶段二：降低开发参与门槛

### 步骤 6：编写开发环境与架构文档

**做什么**

创建开发者文档：

- `docs/DEVELOPMENT.md`
- `docs/ARCHITECTURE.md`
- 必要时创建 `docs/DATA_FORMAT.md`

开发文档至少覆盖：

- Windows 10/11 开发环境。
- 推荐 Node LTS、npm、Rust stable、Tauri CLI 和 Visual Studio Build Tools 版本。
- 安装依赖、运行开发窗口、执行测试和生产构建的命令。
- 系统旧版 Node 造成 Vite/Vitest 失败时的诊断方法。
- 应用数据目录、日志目录和测试数据清理方式。

架构文档至少覆盖：

- React 前端与 Rust command 的调用边界。
- 导入、预览、应用、替换、分配和删除的数据流。
- `library.json`、备份和原子写入策略。
- Windows 当前用户注册表修改范围。
- CUR、ANI、INF 解析与预览缓存。
- 托盘、单实例、启动日志和失败降级。

#### 执行结果（2026-07-14）

步骤状态：**已完成**

- 已创建 `docs/DEVELOPMENT.md`，覆盖 Windows 10/11、Node.js 24 LTS、npm、Rust stable MSVC、Tauri CLI、Visual Studio Build Tools、WebView2、依赖安装、浏览器与桌面开发模式、测试、生产构建、日志和安全清理测试数据。
- 已创建 `docs/ARCHITECTURE.md`，说明 React 与 Rust command 边界，以及导入、预览、应用、替换、分配、删除、JSON 恢复、注册表、托盘、单实例和启动降级数据流。
- 已创建 `docs/DATA_FORMAT.md`，记录应用数据目录、`library.json`、`settings.json`、15 个 Windows 角色、预览缓存、备份恢复、兼容性规则和数据不变量，并明确该格式不是稳定公共 API。
- 已在 README 和 `CONTRIBUTING.md` 增加三份开发者文档入口，并将旧 Node 20 建议统一为 Node.js 24 LTS。
- 已验证所有新增本地 Markdown 链接存在，文档不包含维护者本机私有路径，三个应用版本文件仍统一为 `0.1.11`。
- 已使用 Node.js `v24.14.0` 通过 6 项前端测试和 TypeScript/Vite 生产构建；`cargo fmt --check`、15 项 Rust 测试、`cargo check` 及 `tauri build --help` 均通过。
- 本步骤只有文档变化，未生成或发布新二进制，因此未提升应用版本。

**解决什么问题**

解决项目只能在原作者电脑运行、贡献者无法理解数据流、误改注册表或破坏文件事务的问题。

**主要产物**

- 开发环境文档。
- 架构和数据流文档。
- 数据格式说明。

**验收标准**

- 在一台干净 Windows 环境中仅按照文档即可启动开发模式并运行测试。
- 文档中的命令与 `package.json`、Cargo 配置一致。
- 不要求贡献者访问维护者本机路径或私有文件。

### 步骤 7：建立版本记录和路线图

**做什么**

创建：

- `CHANGELOG.md`
- `ROADMAP.md`

同步整理现有版本记录，并明确：

- 当前稳定版本。
- 下一个功能或 Bug 修复版本从 `0.1.12` 开始。
- 文档和社区配置变更不单独提升应用版本。
- 任何生成新二进制或改变应用行为的修改都必须提升版本。
- MVP 范围和明确不做的功能。
- 已知限制和候选优化方向。

#### 执行结果（2026-07-14）

步骤状态：**已完成**

- 已创建根目录 `CHANGELOG.md`，建立“未发布”区，并记录首个具有可验证公开标签的 `v0.1.11`，发布日期为 2026-07-13。
- 已将 `0.1.10` 角色替换功能和 `0.1.1` 图标改进标注为未单独公开发布的开发里程碑，不补造标签、Release 或发布日期。
- 已创建根目录 `ROADMAP.md`，明确当前 MVP、计划中的社区质量建设、研究中的数据安全和兼容性优化，以及在线市场、账号、云同步和跨平台等不在范围内的功能。
- 已重写 `docs/版本管理.md`，统一四个版本源、二进制版本递增条件、发布同步步骤、最新版本支持策略和公开标签不可移动规则。
- 已在 README 增加稳定版本、更新日志和路线图入口；README、SECURITY、版本文档、四个版本源与 `v0.1.11` 标签保持一致。
- 已确认 `package.json`、`package-lock.json` 根包、`Cargo.toml`、`Cargo.lock` 和 `tauri.conf.json` 均为 `0.1.11`；下一次产生新二进制的修改从 `0.1.12` 开始。
- 已验证全部新增本地 Markdown 链接、UTF-8 内容和隐私路径检查；本步骤未修改应用行为或生成二进制，因此未提升版本。

**解决什么问题**

解决版本历史散落、用户不知道升级内容、贡献者重复提出不在范围内的需求，以及版本号不同步的问题。

**主要产物**

- `CHANGELOG.md`
- `ROADMAP.md`
- 清晰的版本支持策略。

**验收标准**

- 每个已发布版本都有日期、主要变化和重要修复。
- 路线图区分“计划”“研究中”“不在范围内”。
- README、Release 和版本文档之间没有冲突。

## 7. 阶段三：建立自动质量门禁

### 步骤 8：建立基础 CI

**做什么**

创建 Windows GitHub Actions 工作流，在 Push 和 Pull Request 时执行：

- 固定 Node LTS 版本并执行 `npm ci`。
- 前端组件测试。
- TypeScript 和 Vite 生产构建。
- Rust 格式检查。
- Rust 测试。
- 基础 Clippy 检查，初期可以作为非阻塞检查。

CI 应采用最小权限、依赖缓存和可读的 Job 名称。普通 PR 不构建约 200 MB 的离线安装包，避免浪费时间和 Actions 存储。

#### 执行结果（2026-07-14）

步骤状态：**已完成**

- 已创建 `.github/workflows/ci.yml`，在 Push 到 `main`、面向 `main` 的 Pull Request 和手动触发时运行，采用 `contents: read` 最小权限与同分支并发取消。
- 已通过 `.nvmrc` 固定 Node.js `24.14.0`；Frontend Job 使用 `actions/setup-node@v6` 的 npm 缓存并执行 `npm ci`、6 项 Vitest 测试和 TypeScript/Vite 生产构建。
- Rust Job 安装 stable MSVC、rustfmt 和 Clippy，使用 `actions/cache@v5` 缓存 Cargo 依赖与构建目录，并阻塞执行格式、15 项测试和编译检查。
- Clippy 已接入但暂时设置为非阻塞；当前可稳定运行并报告 8 条既有警告，严格 `-D warnings` 留到步骤 9 清理后启用。
- 工作流使用 `actions/checkout@v6`，Job 名称为 `Frontend / Test and build` 与 `Rust / Format, test, and check`，普通 PR 不调用 Tauri 打包，也不上传约 200 MB 的离线安装包。
- YAML 已通过 Prettier 解析检查，本地 Node.js 24.14.0、前端和 Rust 全部阻塞命令均通过，文档与贡献指南已同步固定版本说明。
- PR #4 的干净基线在两个 GitHub 托管 Windows Runner Job 中全部通过；随后提交临时 rustfmt 错误，Frontend 继续通过且 Rust 正确失败，删除探针后当前 HEAD 再次全部通过。
- 已在 README 增加 Windows CI 状态徽章，并在 CHANGELOG 和 ROADMAP 更新自动质量门禁状态；应用版本保持 `0.1.11`，未生成发布二进制。

**解决什么问题**

解决“只在维护者电脑能运行”、错误代码直接进入主分支，以及贡献者无法知道失败原因的问题。

**主要产物**

- `.github/workflows/ci.yml`
- README 中的 CI 状态入口，可在工作流稳定后再增加徽章。

**验收标准**

- 干净分支上的前端和 Rust 检查全部通过。
- 人为制造一个测试或格式错误时，CI 能正确失败。
- CI 不依赖本机缓存、私有目录或交互式窗口。

### 步骤 9：清理并启用严格代码质量检查

**做什么**

在基础 CI 稳定后建立统一质量标准：

- 清理现有 Clippy 警告。
- 将 `cargo clippy --all-targets -- -D warnings` 设为阻塞检查。
- 为 React 和 TypeScript 增加 ESLint 规则。
- 选择并固定前端格式化规则。
- 增加统一的 `lint`、`format:check` 等项目脚本。
- 对共享行为和高风险 Windows 操作逐步补充测试。

**解决什么问题**

解决代码风格漂移、警告长期累积、不同贡献者使用不同检查方式，以及 CI 只能发现编译错误的问题。

**主要产物**

- 干净的 Clippy 结果。
- 前端 Lint 和格式配置。
- CI 中稳定的质量检查名称。

**验收标准**

- 本地和 CI 使用相同命令得到相同结果。
- 新增警告、格式问题或明显的 TypeScript 规则问题会阻止合并。
- 清理警告过程中不改变未授权的产品行为。

### 步骤 10：建立依赖与供应链安全

**做什么**

创建 `.github/dependabot.yml`，覆盖：

- npm。
- Cargo。
- GitHub Actions。

同时配置或评估：

- Dependabot Alerts。
- Dependabot Security Updates。
- npm 依赖审计。
- Rust 漏洞和许可证审计。
- TypeScript 代码扫描。
- GitHub Actions 依赖固定与最小权限。
- 第三方依赖许可证清单。

Dependabot 更新建议按周运行，并限制同时打开的 PR 数量，避免维护者被更新请求淹没。

**解决什么问题**

解决依赖长期不更新、已知漏洞无人发现、Action 依赖失控和第三方许可证不兼容的问题。

**主要产物**

- `.github/dependabot.yml`
- 已启用的 Dependabot 安全功能。
- 依赖与许可证审计结果。

**验收标准**

- GitHub 能识别 npm、Cargo 和 Actions 三类依赖。
- Dependabot 检查能够正常运行且不会一次创建过多 PR。
- 高严重性依赖漏洞有明确处理流程。

## 8. 阶段四：保护主分支和发布历史

### 步骤 11：配置分支与标签 Ruleset

**做什么**

在 CI 检查名称稳定后，为 `main` 配置规则：

- 外部修改必须通过 Pull Request。
- 合并前必须通过前端和 Rust CI。
- 阻止 Force Push 和分支删除。
- 要求解决所有审查对话。
- 单维护者阶段可保留受控的管理员紧急绕过权限。

为 `v*` 发布标签配置规则：

- 阻止修改或强制移动已经发布的版本标签。
- 限制可以创建发布标签的角色。

**解决什么问题**

解决未经检查的代码直接进入主分支、历史被重写、已发布标签被移动，以及 Release 无法追溯的问题。

**主要产物**

- `main` 分支 Ruleset。
- `v*` 标签 Ruleset。

**验收标准**

- 普通分支不能绕过 CI 直接合并。
- Force Push 到 `main` 和已发布标签会被拒绝。
- 维护者仍有记录清晰的紧急处理方式。

## 9. 阶段五：规范 Windows 发布

### 步骤 12：建立自动化 Release

**做什么**

创建独立发布工作流，建议通过版本标签或手动触发：

- 验证 `package.json`、`package-lock.json`、`Cargo.toml`、`tauri.conf.json` 和版本文档一致。
- 执行全部前端和 Rust 检查。
- 在 Windows Runner 构建 Tauri Release。
- 生成安装版、便携版和 SHA-256。
- 统一无空格、稳定的小写连字符文件名。
- 创建 Draft Release 并上传资产。
- 根据 `CHANGELOG.md` 生成 Release Notes。
- 维护者下载并冒烟验证后，人工发布为 Latest Release。

首次实现时先通过手动 Dry Run 或预发布标签验证，不直接覆盖现有 `v0.1.11`。

**解决什么问题**

解决手工打包步骤遗漏、版本号不一致、Release 文件名被 GitHub改写、校验文件失配和错误二进制直接公开的问题。

**主要产物**

- `.github/workflows/release.yml`
- 自动生成的 Draft Release。
- 可重复的发布日志和构建产物。

**验收标准**

- 同一提交可以重复得到结构一致的发布文件。
- 安装版、便携版和校验清单名称完全一致。
- SHA-256 可以校验线上下载文件。
- 没有签名密钥或 GitHub Token 出现在日志和 Artifact 中。

### 步骤 13：评估并接入 Windows 代码签名

**做什么**

按以下顺序评估：

1. SignPath Foundation 等符合条件的开源项目签名方案。
2. Microsoft Store 分发。
3. Azure Artifact Signing。
4. 传统 OV 代码签名证书。

确认方案后：

- 通过 OIDC、受保护环境或 GitHub Secrets 接入签名。
- 不把 `.pfx`、密码或私钥提交到仓库。
- 在生成 SHA-256 前完成 EXE 和安装包签名。
- 使用系统工具验证签名、时间戳和发布者名称。
- 在 README 和 Release Notes 中说明签名状态。

**解决什么问题**

解决 Windows 显示未知发布者、企业环境阻止未签名程序、用户难以判断安装包来源的问题。

**主要产物**

- 已确认的签名方案。
- 受保护的签名流程。
- 已签名的发布文件和验证记录。

**验收标准**

- Windows 文件属性能显示预期发布者。
- 签名和时间戳验证通过。
- 新版本更换文件哈希后仍可能出现 SmartScreen 信誉提示，文档不得承诺签名后一定没有提示。

### 步骤 14：记录发布和回滚流程

**做什么**

创建 `docs/RELEASING.md`，记录：

- 版本选择和升级规则。
- 发布前检查清单。
- 创建标签和 Draft Release 的顺序。
- 安装版与便携版的冒烟测试。
- 签名和 SHA-256 验证。
- Release 发布和公告步骤。
- 发现严重问题后的撤回、标记和修复版本流程。
- 禁止移动已经公开的版本标签。

**解决什么问题**

解决发布知识只存在维护者记忆中、紧急修复时误删历史，以及后续维护者无法复现发布的问题。

**主要产物**

- `docs/RELEASING.md`
- 可复用的发布检查清单。

**验收标准**

- 另一名维护者仅按照文档即可完成一次 Draft Release。
- 回滚流程不会覆盖旧标签或静默替换已下载文件。

## 10. 阶段六：建立社区运营节奏

### 步骤 15：建立标签和问题分流规则

**做什么**

建立最小标签集合：

- `bug`
- `enhancement`
- `documentation`
- `security`
- `cursor-format`
- `windows`
- `good first issue`
- `help wanted`
- `needs reproduction`
- `needs decision`
- `wontfix`

定义维护者处理流程：确认、分类、补充信息、接受、关闭和发布关联。选择少量边界清楚的任务标记为 `good first issue`。

**解决什么问题**

解决 Issue 数量增加后无法检索、重复问题长期堆积、新贡献者找不到合适任务的问题。

**主要产物**

- GitHub 标签集合。
- Issue 分流说明。
- 首批适合贡献者的任务。

**验收标准**

- 新 Issue 可以在一次分流中获得类型和状态标签。
- `good first issue` 不涉及注册表、文件删除或发布密钥等高风险区域。

### 步骤 16：执行社区发布前总验收

**做什么**

完成一次从访客、贡献者、维护者和最终用户四个视角的验收：

- 访客能理解项目、许可、下载方式和反馈入口。
- 贡献者能从干净克隆完成安装、测试和 PR。
- 维护者能处理 Issue、私密漏洞和依赖更新。
- 最终用户能下载、校验、安装、运行和找到日志。
- CI、Ruleset、Dependabot 和 Draft Release 流程均实际运行一次。
- 检查仓库中没有密钥、个人路径、构建目录和超大二进制。

**解决什么问题**

解决文件看似齐全但链接失效、流程从未演练、权限设置互相阻塞，以及真正有贡献者时才发现无法使用的问题。

**主要产物**

- 一份社区发布验收记录。
- 尚未解决问题及风险清单。
- 对外发布公告草案。

**验收标准**

- GitHub Community Profile 的核心项目完整。
- 一次测试 PR 能从创建到 CI 通过并合并。
- 一次测试漏洞报告能私密到达维护者。
- 一次发布 Dry Run 能生成正确资产但不会自动公开。

### 步骤 17：建立长期维护周期

**做什么**

建立轻量维护节奏：

| 周期 | 维护任务 |
| --- | --- |
| 每周 | 分流 Issue、检查安全报告、处理 Dependabot PR |
| 每月 | 检查依赖、CI 稳定性、失效链接和待确认路线图 |
| 每次发布 | 更新版本、CHANGELOG、测试、签名、校验和 Release Notes |
| 每季度 | 复查支持版本、路线图、社区规则和维护者权限 |

**解决什么问题**

解决社区文件创建后长期失效、依赖告警堆积、路线图失真和维护工作完全依赖临时记忆的问题。

**主要产物**

- 可执行的维护周期。
- 定期复查记录或 GitHub Project 任务。

**验收标准**

- 每类维护任务都有负责人或默认维护者。
- 不设置维护者无法长期兑现的硬性响应 SLA。

## 11. 推荐执行批次

为降低一次性改动风险，建议按以下批次执行：

| 批次 | 包含步骤 | 建议提交或 PR |
| --- | --- | --- |
| A：社区基础 | 0 至 5 | `docs: add community contribution and support files` |
| B：开发文档 | 6 至 7 | `docs: add development, architecture, and roadmap guides` |
| C：基础自动化 | 8 | `ci: add Windows validation workflow` |
| D：质量与安全 | 9 至 10 | 分成质量清理和依赖安全两个 PR |
| E：仓库保护 | 11 | GitHub 设置变更，单独记录前后状态 |
| F：发布体系 | 12 至 14 | 自动发布、签名和发布文档分别审查 |
| G：社区上线 | 15 至 17 | 标签、验收和维护节奏 |

## 12. 总体验收标准

全部步骤完成后，项目应满足：

1. 用户、贡献者、安全研究人员和维护者都有明确入口。
2. 外部贡献可以在干净环境构建和测试。
3. 所有 PR 在进入 `main` 前通过自动检查。
4. 依赖漏洞和版本更新可以被持续发现。
5. `main` 和版本标签受到保护。
6. Release 可以稳定生成安装版、便携版和匹配的 SHA-256。
7. 签名材料、令牌和用户隐私不会进入仓库或日志。
8. 版本、CHANGELOG、README 和 Release Notes 保持一致。
9. 社区运营规则足够清楚，同时不过度承诺维护者资源。
10. 项目仍保持本地 Windows 光标管理 MVP 定位，不因社区建设扩展非目标功能。

## 13. 参考资料

- [GitHub：建立健康的贡献社区](https://docs.github.com/en/communities/setting-up-your-project-for-healthy-contributions)
- [GitHub：贡献指南](https://docs.github.com/en/enterprise-cloud@latest/communities/setting-up-your-project-for-healthy-contributions/setting-guidelines-for-repository-contributors)
- [GitHub：安全策略](https://docs.github.com/en/code-security/how-tos/report-and-fix-vulnerabilities/configure-vulnerability-reporting/add-security-policy)
- [GitHub：私密漏洞报告](https://docs.github.com/en/code-security/how-tos/report-and-fix-vulnerabilities/configure-vulnerability-reporting/configure-for-a-repository)
- [GitHub：Dependabot](https://docs.github.com/en/code-security/concepts/supply-chain-security/dependabot-version-updates)
- [GitHub：仓库 Rulesets](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/available-rules-for-rulesets)
- [Microsoft：Windows 代码签名选项](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options)
- [Microsoft：SmartScreen 信誉机制](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation)
